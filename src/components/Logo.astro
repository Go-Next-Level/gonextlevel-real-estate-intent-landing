---
interface Props {
  href?: string;
  showWordmark?: boolean;
}
const { href = "/", showWordmark = true } = Astro.props;
---
<a href={href} aria-label="Real Estate Intent — home" style="display:inline-flex; align-items:center; gap:10px;">
  <span class="logo-mark" aria-hidden="true">
    <img src="/logo-mark.png" alt="" width="32" height="32" loading="eager" decoding="async" />
  </span>
  {showWordmark && (
    <span style="display:flex; flex-direction:column;">
      <span class="wordmark-1">RealEstate<span style="color:#3B82F6;">Intent</span></span>
      <span class="wordmark-2">Audience intelligence</span>
    </span>
  )}
</a>
